#include <stdio.h>

int main() {
    int n;

    // Read input number
    scanf("%d", &n);

    // Print factors of the number
    for (int i = 1; i <= n; i++) {
        if (n % i == 0) {
            printf("%d ", i);
        }
    }

    printf("\n"); // Newline after output
    return 0;
}
