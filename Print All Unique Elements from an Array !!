#include <stdio.h>

int main() {
    int N;
    
    // Read number of elements
    scanf("%d", &N);
    
    int arr[N];
    
    // Read array elements
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    int foundUnique = 0; // To track if any unique element exists

    // Loop through each element
    for (int i = 0; i < N; i++) {
        int count = 0;

        // Count frequency of arr[i]
        for (int j = 0; j < N; j++) {
            if (arr[i] == arr[j]) {
                count++;
            }
        }

        // If element occurs only once, print it
        if (count == 1) {
            if (foundUnique)
                printf(" "); // Add space before next element
            printf("%d", arr[i]);
            foundUnique = 1;
        }
    }

    // If no unique element found
    if (!foundUnique) {
        printf("No unique elements");
    }

    return 0;
}
