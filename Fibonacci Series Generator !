#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);  // Input

    int a = 0, b = 1, next;

    for (int i = 0; i < N; i++) {
        if (i == 0) {
            printf("%d", a);
        } else if (i == 1) {
            printf(" %d", b);
        } else {
            next = a + b;
            printf(" %d", next);
            a = b;
            b = next;
        }
    }

    printf("\n");  // Newline after output
    return 0;
}
