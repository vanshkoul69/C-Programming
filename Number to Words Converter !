#include <stdio.h>
#include <string.h>

void printDigitWord(char digit) {
    switch (digit) {
        case '0': printf("Zero "); break;
        case '1': printf("One "); break;
        case '2': printf("Two "); break;
        case '3': printf("Three "); break;
        case '4': printf("Four "); break;
        case '5': printf("Five "); break;
        case '6': printf("Six "); break;
        case '7': printf("Seven "); break;
        case '8': printf("Eight "); break;
        case '9': printf("Nine "); break;
        default: break; // shouldn't happen
    }
}

int main() {
    char number[100];  // to store the input number as a string

    // Read the number as a string to handle leading zeros
    scanf("%s", number);

    // Traverse each character and print the corresponding word
    for (int i = 0; number[i] != '\0'; i++) {
        printDigitWord(number[i]);
    }

    printf("\n");
    return 0;
}
