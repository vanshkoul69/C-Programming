#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);  // Input

    int sum = 0;

    // Calculate the sum using a loop
    for (int i = 1; i <= N; i++) {
        sum += i;
    }

    printf("%d\n", sum);  // Output

    return 0;
}
