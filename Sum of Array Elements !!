#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    int arr[100];
    int sum = 0;

    // Read N elements into the array
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
        sum += arr[i];  // Add element to sum
    }

    // Print the sum of all elements
    printf("%d\n", sum);

    return 0;
}
