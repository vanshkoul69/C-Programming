#include <stdio.h>
#include <math.h>

int main() {
    int n, isPrime = 1;

    // Read the input number
    scanf("%d", &n);

    // Handle edge cases
    if (n <= 1) {
        isPrime = 0;
    } else {
        // Check divisibility up to sqrt(n)
        for (int i = 2; i <= sqrt(n); i++) {
            if (n % i == 0) {
                isPrime = 0;
                break;
            }
        }
    }

    // Output the result
    if (isPrime)
        printf("Prime\n");
    else
        printf("Not Prime\n");

    return 0;
}
