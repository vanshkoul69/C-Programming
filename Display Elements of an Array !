#include <stdio.h>

int main() {
    int N;

    // Read the number of elements
    scanf("%d", &N);

    // Declare the array with maximum size 100
    int arr[100];

    // Read N integers into the array
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    // Print all array elements separated by a space
    for (int i = 0; i < N; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
